a=$(cat $* | wc -l)
while (cat $a -eq 0); do
  if [${a} -gt 3]; then
    echo "(cat $* | head -3)"
  read -p "Pressione enter: " a
  if [-z ${a}]; then
    echo "echo "(cat $* | head -3)"
  fi
done
   


