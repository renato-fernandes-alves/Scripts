function maior {
	var=""
	for i in $*;do
    var=$(echo $i)
		if [ $var -lt $i ];then
			var=$(echo $i)
		fi
  done
	echo $var
}

function menor {
  var=""
	for i in $*;do
		var=$(echo $i)
		if [ $var -gt $i ];then
			var=$(echo $i)
		fi
  done
	echo $var
}
