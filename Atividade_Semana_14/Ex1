function maior {
	var=""
	for i in $*;do
    valor=$(echo $i)
		if [ $var -lt $i ];then
			var=$(echo $i)
		fi
  done
	echo $var
}

function menor {
  valor=""
	for i in $*;do
		var=$(echo $i)
		if [ $var -gt $i ];then
			var=$(echo $i)
		fi
  done
	echo $var
}
